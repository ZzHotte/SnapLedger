# SnapLedger — 架构设计 v3

> v2 基础上根据三个关键决策更新：① 社交属性 = 纯展示性静态面板，不做真人比较；
> ② 支持最多 5 人的共享账本（邀请好友/家人共同记账，可分权限）；
> ③ 前端定为 Next.js PWA——同时满足"能装在 iPhone 上日常用"和"能挂在网上当作品集 demo"两个目的。

## 一句话定位

拍一张小票 → AI 自动识别金额/商家/品类 → 一键确认入账（个人账本或与最多 4 位好友/家人共享的账本）
→ 仪表盘看到消费趋势；顺带看看外汇、GDP、各国工资水平这类宏观数据做参照；未来加上 AI 理财建议。

---

## 核心功能模块（按优先级）

### 1️⃣ 核心：小票拍照记账 + 共享账本

流程：拍照/相册选图 → AI 提取结构化字段 → **用户确认/修正** → 入库（写入某个账本）→ 仪表盘展示

**共享账本设计**（已确认需求）：
- 一个账本最多 5 人，创建者是 owner，可邀请其他人加入，逐人设置权限：
  - **仅查看**：能看仪表盘和明细，不能记账/改数据
  - **可编辑**：能上传小票、记账、改分类
- 一个用户可以同时属于多个账本（比如"个人账本" + "和室友合租账本" + "家庭账本"），
  前端需要一个账本切换器（类似 Slack 切换 workspace）。
- 账本内的分类、预算都挂在账本下，而不是挂在单个用户下——这样共享账本里所有人看到的分类体系一致。

关键设计原则不变：**AI 提取结果不能自动入账**，必须有人工确认环节。

### 2️⃣ 辅助：宏观财经数据看板（纯展示，不做社交比较）

已确认："社交属性"只是想让 app 更好玩、信息量更丰富，**不需要做真人对比/好友排行**这类功能，
只需要几个静态信息面板：
- 各国银行存款利率（活期/定期）
- 外汇兑换汇率
- 各国人均 GDP / 平均工资

这大大简化了这块——不涉及匿名化、隐私聚合这些复杂问题，就是几个"数据展示卡片"，
定时爬/拉数据存库，前端渲染即可，跟社交网络意义上的"社交功能"没有关系。

### 3️⃣ 后续拓展：AI 理财规划

基于账本的 `transactions`（分类聚合）+ 用户设定的收入 + 宏观数据，用 LLM 生成月度收入分配建议
和消费分析报告。依赖前两层积累的数据，做成第三阶段合理，不提前设计细节。

---

## 前端技术栈决策：Next.js PWA

**结论：用 Next.js（React + TypeScript + Tailwind）做成 PWA，不做 React Native / 原生 App。**

理由对应你的三个约束：
1. **只会写 React、电脑开发、很久没写代码** → Next.js 就是标准 Web React，没有额外要学的组件模型；
   React Native 虽然也叫 "React"，但组件（`View`/`Text`/`StyleSheet`）、打包发布（Xcode、
   Apple 开发者账号、TestFlight/App Store 审核）都是另一套东西，在"捡回手感"阶段属于不必要的负担。
2. **想放网上做作品集 demo** → Next.js 项目本身就是网站，`npm run build` 部署到 Vercel 免费层就有一个
   永久在线的 URL，招聘方点开链接直接能用，比"需要装 TestFlight 才能体验"的原生 App 摩擦小得多。
3. **想装在 iPhone 上日常用** → PWA 在 iOS Safari 里可以"添加到主屏幕"，图标、启动画面、全屏体验
   和原生 App 几乎无区别；拍小票用 `<input type="file" capture="environment">` 直接调后置摄像头，
   够用。

**要接受的 PWA 局限（如实告知，不是隐藏坑）**：
- 推送通知需要 iOS 16.4+（现在的 iPhone 基本都满足，不是问题）
- 离线能力比原生弱一些（可以用 Service Worker 做"离线拍照排队上传"，但不追求完整离线可用）
- 没有 App Store 入口——对个人日常用 + 作品集展示这两个目的来说，这其实不是缺点

**什么情况下值得换成 React Native/Expo**：如果以后你明确想要"App Store 里能搜到"这件事本身
（比如想体验一次完整的发布流程，或者交互复杂度上去了需要原生手势/动画），再迁移不迟——
到时候 `transactions`/`ledgers` 这些后端和数据结构完全不用动，只是换一层前端。现在不必为这个可能性
预先设计。

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | **Next.js（PWA）+ TypeScript + Tailwind** | 见上方决策说明 |
| 后端 | Python FastAPI | AI/OCR 生态成熟，异步性能够用 |
| 数据库 | PostgreSQL | 记账数据强结构化，关系型合适；JSONB 存 AI 原始提取结果 |
| 图片存储 | Cloudinary | 小票原图保留，供复核和重新提取；免费额度大（约 25GB 存储+流量/月），自带自动压缩/EXIF 转正/缩略图，省掉自己写图片处理逻辑 |
| 缓存/定时任务 | Redis + APScheduler | 缓存汇率/宏观数据，定时拉取更新 |
| AI 服务 | Gemini API（多模态） | 小票图片直接提取结构化字段；理财建议生成。小票提取用 Gemini Flash（便宜量大够用），理财建议/月度分析用 Gemini Pro（推理更强） |
| 部署 | Vercel（前端）+ Railway/Render 免费层（后端+DB） | 前后端分开部署，前端要有稳定公开 URL 用于展示 |

---

## 数据库表设计（v3，加入共享账本）

```
users              — id, email, password_hash, base_currency, created_at

ledgers            — id, name, owner_id, created_at
                      # 一个账本 = 一份共享的账，最多 5 名成员

ledger_members     — id, ledger_id, user_id, role(owner/editor/viewer),
                      status(invited/active), invited_by, joined_at
                      # 唯一约束 (ledger_id, user_id)；应用层限制单账本成员数 ≤ 5

ledger_invites     — id, ledger_id, invite_code, role(editor/viewer),
                      created_by, expires_at, used_by(可空), used_at(可空)
                      # 生成一个邀请链接/邀请码，对方打开即可加入

categories         — id, ledger_id, name, parent_id(可空), icon, is_default
                      # 分类挂账本，共享账本内所有成员看到同一套分类

transactions       — id, ledger_id, created_by(user_id), amount, currency,
                      category_id, merchant, transaction_date, note,
                      source(receipt/manual), receipt_id(可空),
                      confirmed(bool), created_at

receipts           — id, ledger_id, uploaded_by(user_id), image_path,
                      ocr_raw_json(JSONB), model_version, confidence,
                      status(pending/confirmed/rejected), uploaded_at

budgets            — id, ledger_id, category_id(可空=总预算), month, planned_amount

exchange_rates     — id, base_currency, target_currency, rate, fetched_at

bank_rates         — id, country_code, bank_name, product_type(活期/定期),
                      term_months(可空), rate, fetched_at, source_url

macro_indicators   — id, country_code, indicator(gdp_per_capita/avg_wage),
                      value, year, source

ai_insights        — id, ledger_id, type(budget_suggestion/monthly_report),
                      content(JSONB), period(如 2026-08), created_at
```

关键变化说明：
- 原来挂在 `user_id` 下的 `transactions`/`categories`/`budgets`/`receipts`/`ai_insights`
  全部改挂 `ledger_id`；谁记的账（`created_by`/`uploaded_by`）单独留一列做审计，不影响归属。
- 每个新用户注册时自动创建一个"个人账本"（`ledgers.owner_id = 自己`，只有自己一个成员），
  这样"个人用" 和"共享用" 是同一套模型，不用为单人场景写特殊逻辑。
- 权限校验落在 API 层：`viewer` 角色的请求，写操作（POST/PATCH/DELETE transactions、receipts）
  一律 403。

---

## 核心流程：小票 → 记账（含账本上下文）

```
1. 用户在某个账本上下文里拍照/选图上传（当前活跃账本由前端状态记住，默认个人账本）
2. 后端校验该用户在此账本的角色是 owner/editor（viewer 不能上传）
3. 存原图 → 创建 receipts 记录（ledger_id、uploaded_by、status=pending）
4. 调用 Gemini 多模态 API 提取：商家/日期/总金额/币种/推测分类
5. 提取结果 + 置信度返回前端，预填确认卡片
6. 用户核对/修改字段 → 确认 → 写入 transactions（ledger_id、created_by）
7. 仪表盘按账本聚合展示；共享账本里所有成员看到同一份数据
```

---

## API 设计（v3）

```
POST   /auth/register /auth/login

# 账本与协作
POST   /ledgers                          # 创建账本（注册时自动创建"个人账本"）
GET    /ledgers                          # 我参与的账本列表
POST   /ledgers/{id}/invites             # 生成邀请码/链接，指定角色(editor/viewer)
POST   /ledgers/invites/{code}/accept    # 好友打开邀请链接加入
PATCH  /ledgers/{id}/members/{user_id}   # 修改成员权限
DELETE /ledgers/{id}/members/{user_id}   # 移除成员（或成员自己退出）

# 记账（均带 ledger_id 上下文，如 header 或 query param）
POST   /receipts/upload
GET    /receipts/{id}
POST   /receipts/{id}/confirm
POST   /receipts/{id}/reprocess

GET    /transactions?ledger_id=&month=2026-08
POST   /transactions
PATCH  /transactions/{id}
DELETE /transactions/{id}

GET    /dashboard/summary?ledger_id=
GET    /budgets?ledger_id=

# 宏观数据看板（纯展示，不区分账本）
GET    /market-data/exchange-rate?base=CNY&target=AUD
GET    /market-data/bank-rates?country=CN
GET    /market-data/macro?country=CN&indicator=avg_wage

# AI（Phase 3）
POST   /ai/monthly-report?ledger_id=
POST   /ai/budget-suggestion?ledger_id=
```

---

## 外部数据源（不变）

| 数据 | 来源 | 备注 |
|---|---|---|
| 外汇汇率 | exchangerate.host / Frankfurter（免费，无 key） | 定时任务每日拉取，Redis 缓存 |
| GDP / 平均工资 | World Bank API | 免费免注册，年度更新即可 |
| 各国银行存款利率 | ⚠️ 没有统一免费 API | MVP 阶段手动整理几个你关心国家的主流银行利率表，定期手动更新，不要假设能低成本自动抓取全球数据 |

---

## AI 理财规划模块（Phase 3，设计不变）

输入：账本近 N 个月的 `transactions`（分类聚合）+ 用户设定收入 + 宏观数据参照
输出：结构化 JSON（收入分配建议 / 月度异常点提示），存入 `ai_insights`，仪表盘可视化对比。
等真实记账数据攒够 2-3 个月再细化 prompt 设计。

---

## 开发路线图（业余时间，按周估算）

1. **Week 1**：脚手架、用户认证、`users`/`ledgers`/`ledger_members`/`categories` 建表，
   注册即自动建个人账本
2. **Week 2**：小票上传 + Gemini 多模态提取 + 确认卡片交互（核心中的核心，多花时间打磨）
3. **Week 3**：`transactions`/`receipts` 落地 + 权限校验（viewer 只读）+ 账本切换 UI
4. **Week 4**：邀请加入账本流程（生成邀请码、接受邀请、权限管理界面）
5. **Week 5**：记账仪表盘（月度趋势、分类占比、预算 vs 实际）
6. **Week 6**：接入汇率 + World Bank 数据 + 手动整理的银行利率表，宏观数据面板
7. **Week 7+**：Vercel 部署上线、PWA 打磨（图标/启动画面/主屏幕安装引导）、
   README/案例研究（面向作品集，讲清楚共享账本的权限设计和 AI 提取的确认机制）
8. **后续**：AI 月度报告 / 理财建议模块

**建议起步顺序不变**：先把"拍照 → 确认 → 入库 → 在个人账本列表里看到这条记录"这个最小闭环做完，
自己用一周，再加共享账本和宏观数据——闭环体验不顺，加协作功能只会让排查问题更难。
